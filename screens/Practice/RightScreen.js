import { View, Text } from "native-base";
import React, { useEffect } from "react";
import { StyleSheet, StatusBar, ScrollView, } from "react-native";

export default function RightScreen({ navigator, route }) {
    

    return (
        <Text>
            hi
        </Text>
    )
}